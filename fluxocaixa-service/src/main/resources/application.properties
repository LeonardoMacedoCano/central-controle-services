# Identificação do serviço
spring.application.name=fluxocaixa-service

# Configuração porta
server.port=${FLUXOCAIXA_PORT:8080}

# Configuração do banco
spring.datasource.url=${FLUXOCAIXA_DB_URL}
spring.datasource.username=${FLUXOCAIXA_DB_USERNAME}
spring.datasource.password=${FLUXOCAIXA_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Liquibase
spring.liquibase.change-log=classpath:db/changelog/master.xml

# Configuração JWT
api.security.token.secret=${JWT_SECRET}
api.security.token.expiration-hours=24

# Fuso horário para serialização de datas
spring.jackson.time-zone=${TIME_ZONE:GMT}

# Configuração do Schema
db.schema=${FLUXOCAIXA_SCHEMA}
spring.jpa.properties.hibernate.default_schema=${FLUXOCAIXA_SCHEMA}
spring.liquibase.defaultSchema=${FLUXOCAIXA_SCHEMA}

# Pool de conexões
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
